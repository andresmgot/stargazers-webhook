service: stargazing

provider:
  name: kubeless
  runtime: nodejs6

plugins:
  - serverless-kubeless

functions:
  stargazers:
    handler: stargazers.handler
    events:
      - http:
          path: /stargazer
